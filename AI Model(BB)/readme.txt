You will need phyton and run this commad:

pip install opencv-python numpy matplotlib


Then secondly you would need to run:

python setup_cascades.py    -- this will download all of the dependencies for 